\* The fix: the HTS rename UPDATE is conditional on the renamer's expected
\* base (metadataLocation + @Version) and bumps @Version atomically; a
\* mismatch updates 0 rows -> 409 -> reload and retry.
\* Expected: all invariants hold.
CONSTANTS
  Writers = {w1, w2}
  MaxCommits = 4
  DivergenceCheck = TRUE
  SharedDedupCache = FALSE
  RenameGuard = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK NoSnapshotLoss MonotonicVersion
