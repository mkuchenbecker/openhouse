=== delta-harness :: typed pipelines @ OpenHouse catalog (all cases) ===

1231 cases: 1208 passed, 23 skipped (5 tagged bugs/skips), 0 failed.

Full-suite wall-time: ~879s baseline (~14.6 min, fresh classpath) for the prior 1215-case run →
~0.6s/case + ~120s fixed startup (gradle resolve + Scala compile + embedded server + Spark). A cached
classpath (default; FORCE_CP=1 to refresh) trims the fixed startup; the +16 cases add only a few seconds
of test time. The full run stays well under 1h — a single-process CI/final-step artifact; day-to-day
iteration is on single-id slices.

Composition includes the DDL surface (DDL-TEST-PLAN.md): schema evolution (ADD COLUMN, rename,
type-widen), table properties (reserved-key rejection + forced-override findings), sort order, rename
table, namespace negatives, policy DDL (retention/history/sharing/replication), CTAS/RTAS contract
(incl. RTAS⊕replication), column tags + GRANT/REVOKE ACL, and the Phase-24 preparation multipliers
(ordered-table × all DML = 318; evolved-schema × delete/update/read = 174).

Plus the control-plane shim (REST to the embedded OpenHouseLocalServer): lock enforcement
(control.lock.enforcement — POST /lock rejects Spark UPDATE, DELETE /lock re-enables); undrop lifecycle
tagged SKIP (embedded soft-delete repo is a stub — REST-FIDELITY-EVAL.md).

Plus the branching/WAP axis (BRANCHING-PLAN.md, format-agnostic → parquet only, 9 cases): direct branch
isolation, spark.wap.branch conf routing, WAP stage→cherrypick publish, DDL-on-branch leak-to-main
characterization (finding G8), branch DML isolation, CREATE TAG / DROP BRANCH (both supported via generic
ref-sync), and WAP negatives (both-id-and-branch, insert-into-nonexistent-branch).

Also folded in the DML-after-DDL fixes: distributionMode / formatVersionForced now write+read a row so
the flag/version is exercised; orderedByMulti asserts the multi-column ordered write; grantShared and the
policy.{sharing,history,retention} tests now read data back after the DDL.

Tagged bugs/skips (SKIP, tracked in BUGS.md): insert.explicitColumns, nested.deleteByNestedField,
ddl.renameColumn (RENAME COLUMN silent no-op), ddl.encryption (KMS plugin in a private repo — asserts
intended behavior, skipped), control.undrop (embedded soft-delete stub — REST-FIDELITY-EVAL.md).
OpenHouse audit findings (missing guards incl. G8 branch-leak + error-message readability) tracked in
AUDIT-FINDINGS.md.

--- prior baseline (DML only) ---
660 cases: 651 passed, 9 skipped (2 tagged bugs), 0 failed. 597s wall.

Composition (behaviors × axes):
  - core DML operations (read/delete/update/merge/insert/overwrite) × 6 layouts
    {unpartitioned, partitioned} × {parquet, orc, avro}
  - + partitioned-only operations (dynamic overwrite, overwritePartitions) × 3 partitioned layouts
  - + 44 mutation operations × 6 merge-on-read layouts   (Copy-on-write vs Merge-on-read)
  - + physical CoW/MoR discriminator × 3 formats: on a single-data-file seed, a strict-subset
    DELETE writes exactly one position-delete file under MoR and none under CoW (parquet/orc/avro)
  - + nested/complex types (NestedTable)  × 3 formats
  - + type-edge coverage (TypesTable)     × 3 formats
  - + partition transforms (identity/bucket/truncate/years/months/days/hours) + rejection negatives
  - + time travel (VERSION/TIMESTAMP AS OF, metadata tables, incremental read)
  - + restore/rollback (rollback_to_snapshot, set_current_snapshot)
  - + negative/contract tests (cardinality violation, conflicting updates, non-deterministic
    predicate, non-existent column, bad partitioning, insert arity) — each asserts the ACTUAL
    typed exception (AnalysisException / etc.) plus a message substring
  - + create.schema × 6 layouts

Tagged bugs (SKIP, tracked in BUGS.md):
  - insert.explicitColumns            (× 6)  partial-column INSERT rejected (CANNOT_FIND_DATA)
  - nested.deleteByNestedField        (× 3)  DELETE on nested struct field crashes (optimizer NPE)

OpenHouse capability findings (Gate #0):
  supported : copy-on-write + merge-on-read; timestamp_ntz; time travel (VERSION/TIMESTAMP AS OF);
              incremental read; rollback_to_snapshot / set_current_snapshot procedures;
              transforms identity/bucket/truncate/years/months/days/hours
  rejected  : partition evolution (ALTER ADD/DROP PARTITION FIELD); void transform; days(date)

Physical MoR note: a MoR DELETE only adds a position-delete file when it matches a STRICT SUBSET
of a data file. When the predicate covers a whole data file, Iceberg legitimately drops the file
(metadata delete, no position delete) — so the discriminator seeds all rows into one file
(COALESCE(1)) and deletes a strict subset to make the physical outcome deterministic.

Run on JDK 17 against the embedded OpenHouseLocalServer + OpenHouseCatalog.
