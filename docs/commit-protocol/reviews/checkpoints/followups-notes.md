# Follow-ups integration — working notes

Start: worktree /home/user/worktrees/tla-fixes reset to origin/claude/tla-driven-commit-fixes @ 6490896.

## Phase 1 items (branch claude/tla-driven-commit-fixes)
- [ ] 1 @CheckReturnValue on renameTableId — spotbugs-annotations 4.8.1 IS an
      `implementation` dep of services/housetables (build.gradle:45), and
      `edu.umd.cs.findbugs.annotations.CheckReturnValue` exists in that jar.
      spotbugs plugin applied repo-wide (build.gradle:67) with ignoreFailures=true.
      -> no new dependency needed.
- [ ] 2 @throws HouseTableConcurrentUpdateException on HouseTableRepository.rename
- [ ] 3 HouseTableRepositoryImplTest: assert decoded param value
- [ ] 4 HtsRepositoryTest#testRenameCaseSensitivity: savedRow.getVersion() + assert 1
- [ ] 5 UserTablesServiceImpl: real cause messages
- [ ] 6 OpenHouseCommitRename.tla header: name base spec branch+path
- [ ] 7 PR description: Testing Done -> table; drop duplicate footer

## Phase 2 items (branch claude/tla-rename-followups, base = phase 1)
- [ ] 8 explicit expectedMetadataLocation param on handler seam
- [ ] 9 H2 fixture conflict-branch coverage
- [ ] 10 RenameHtsTokenless TLA action + TLC + README three modes

## PHASE 1 DONE — pushed 9e10d5b
Commits on claude/tla-driven-commit-fixes (on top of 6490896):
- f22ea9a fix: put real conflict context in HTS rename 409 cause messages  (item 5)
- 408ce70 refactor: make the rename conflict contract enforceable at the seams (items 1,2)
- bb09e08 test: assert the rename token's value and the row's real version (items 3,4)
- 9e10d5b docs: name where the rename model's base spec lives (item 6)
Validation: :services:housetables:test + :iceberg:openhouse:internalcatalog:test BUILD SUCCESSFUL
(7m50s); spotlessCheck + checkstyleMain/Test on both modules BUILD SUCCESSFUL (only pre-existing
warnings; the one on UserTableHtsJdbcRepository:132 is the pre-existing long JPQL line).
Item 7: PR #36 description updated (Testing Done -> 9-row table; dropped the duplicate
"_Generated by_" footer, kept the "Generated with [Claude Code]" block + session url).
Resolved 6 review threads (items 1-6). Left unresolved: 3 blocking threads (fixed by prior
commit, not by me) + the 2 phase-2 threads + the README "lead with the result" thread.

## PHASE 2 starting
Worktree /home/user/worktrees/tla-followups on claude/tla-rename-followups (from 9e10d5b).
- item 8 DONE (code): HouseTablesApiHandler.renameEntity gains String expectedMetadataLocation;
  4 impls + mock updated; UserHouseTablesController no longer stuffs the token into
  fromUserTable.metadataLocation. New test UserTableHtsApiHandlerTest (2 cases) pins that the
  parameter, not the DTO field, drives the check.
- item 9 DONE (code): RepositoryTest#testHouseTablesFixtureRenameMirrorsHtsTokenSemantics (Spring,
  real services/tables fixture, 3 modes) + new HouseTablesH2RepositoryRenameParityTest covering
  BOTH fixtures (services/tables + tables-test-fixtures twin) via Mockito CALLS_REAL_METHODS over
  an in-memory row map. All 3 PASS.
  NOTE for report/PR: the *preferred* no-stub catalog-driven e2e is NOT constructible. Iceberg
  refreshes the ops before the commit lands (BaseTransaction.commitSimpleTransaction /
  PropertiesUpdate.apply both call ops.refresh()), so the rename's declared base is re-freshened
  from the advanced HTS row and no stale token ever reaches the fixture. Verified empirically:
  a first attempt (advance the row between loadTable and commitTransaction) committed cleanly.
  This is itself a useful narrowing result about the real-world window.

## PHASE 2 DONE — pushed 511a6a8, PR #37 (draft, base claude/tla-driven-commit-fixes)
- item 10 DONE: RenameToken constant, rSeen var, RenameHtsRead/RenameHtsTokenless/
  RenameHtsTokenlessConflict actions; rename_tokenless.cfg -> NoSnapshotLoss VIOLATED
  (351 distinct, depth 8); rename_tokenless_version.cfg -> NO ERROR (9806 distinct,
  complete graph) => MonotonicVersion HOLDS in tokenless mode. All 5 logs committed.
  README rewritten (three modes, leads with result -> also closes comment 12).
- Validation: 3 module suites 619 PASSED / 0 FAILED; spotless + checkstyle clean.
- Report written to reports/15-followups.md.
