\* Current code: post-fix commit path (divergence check ON), but the HTS
\* rename UPDATE is unconditional (no @Version predicate, no bump).
\* Expected: NoSnapshotLoss is VIOLATED (rename clobbers a racing commit).
CONSTANTS
  Writers = {w1, w2}
  MaxCommits = 4
  DivergenceCheck = TRUE
  SharedDedupCache = FALSE
  RenameGuard = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK NoSnapshotLoss
