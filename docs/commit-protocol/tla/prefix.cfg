\* PRE-FIX protocol: no abortIfWriterBaseDivergedFromCatalog; writers routed
\* to distinct Tables-service replicas (per-JVM failIfRetryUpdate caches).
\* Expected: NoSnapshotLoss VIOLATED (bug #612 / incident-12185).
CONSTANTS
  Writers = {w1, w2}
  MaxCommits = 4
  DivergenceCheck = FALSE
  SharedDedupCache = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK NoSnapshotLoss MonotonicVersion
