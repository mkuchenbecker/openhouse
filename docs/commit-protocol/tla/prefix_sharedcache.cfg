\* Sensitivity check: PRE-FIX protocol but all writers hit ONE
\* Tables-service JVM, so failIfRetryUpdate's shared Guava cache
\* accidentally serializes same-base writers. Expected: invariants hold
\* WITH THESE CONSTANTS -- but this is an artifact (the real cache has a
\* 5-min TTL and 1000-entry cap, not modeled), not a correctness mechanism.
CONSTANTS
  Writers = {w1, w2}
  MaxCommits = 4
  DivergenceCheck = FALSE
  SharedDedupCache = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK NoSnapshotLoss MonotonicVersion
